   # Dependencies
   /node_modules

   # Compiled TypeScript files
   /dist
   /build

   # Log files
   logs
   *.log
   npm-debug.log*
   yarn-debug.log*
   yarn-error.log*
   pids
   *.pid
   *.seed
   *.pid.lock

   # Environment variables
   .env
   .env.test
   .env.development
   .env.production

   # Optional Editor files
   .vscode/
   .idea/
   *.sublime-project
   *.sublime-workspace

   # OS generated files
   .DS_Store
   .DS_Store?
   ._*
   .Spotlight-V100
   .Trashes
   ehthumbs.db
   Thumbs.db